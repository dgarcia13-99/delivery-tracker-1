<head>
  <style>
  
  .waiting_on{
    background-color:#fafad2;
  }

  .received{
    background-color:#90ee8f;
  }
  </style>
</head>

<body>

<div>
  <div>
    <h1>
      Delivery Tracker
    </h1>

    <p>Know if something gets lotst in the mail.</p>
  </div>
</div>

<hr>

<div>
  <div>
    <h2>
      Expecting a package?
    </h2>

    <form action="/insert_delivery" method="post">
      <div>
        <label for="description_box">
          Description
        </label>

        <input type="text" id="description_box" name="query_description">
      </div>

      <div>
        <label for="supposed_to_arrive_on_box">
          Supposed to arrive on
        </label>

        <input type="date" id="supposed_to_arrive_on_box" name="query_supposed_to_arrive_on">
      </div>

      <div>
        <label for="details_box">
          Details
        </label>

        <textarea type="text" id="details_box" name="query_details" rows="3"></textarea>
      </div>

      <button>
        Log delivery
      </button>
    </form>
  </div>
</div>

<hr>

<div class="waiting_on">
  <h2>Waiting on</h2>

<ul>
<% @list_of_deliveries.where(:user_id => current_user.id).where(:arrived => false).each do |a_delivery| %>
  <li><%=a_delivery.description%>
    <%if a_delivery.supposed_to_arrive_on < Time.now%>
      <div style="color:red;">Supposed to arrive on <%=a_delivery.supposed_to_arrive_on%><div>
    <%else%>
      <div>Supposed to arrive on <%=a_delivery.supposed_to_arrive_on%><div>
    <%end%>
  <div><%=a_delivery.details%><div>
  <div>
    <form action="modify_delivery/<%=a_delivery.id%>" method="post">
    <input type="hidden" name="received" value="true">
      <button>Mark as received</button>
    </form>
  </div>
  <br>
  </li>
<%end%>
</ul>
</div>

<hr>

<div class="received">
  <h2>Received</h2>

<ul>
<% @list_of_deliveries.where(:user_id => current_user.id).where(:arrived => true).each do |a_delivery| %>
  <li><%=a_delivery.description%>
    <div><small><%=a_delivery.updated_at%> <a href="/delete_delivery/<%=a_delivery.id%>"><small>Delete</a><div>
  </li>
<%end%>
</ul>
</div>

</body>     
